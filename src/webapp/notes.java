package webapp;

public class notes {
    /*add = I think its commit same job because didn't see that add
checkout = when u are working on a specific branch you must check-out it (it works the opposite of the English meaning of that word )
branch = is when basically your own version of the local repository that you will be working at and once its done you can merge it to the master repository (to avoid any mess )
push = its the share of the local repository work to the remote repository of the origin master (after asking the permeation to merge )

     */
}
